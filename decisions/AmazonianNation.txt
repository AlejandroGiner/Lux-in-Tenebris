country_decisions = {
	
	amazonia_nation = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			is_nomad = no
			is_random_new_world = no			
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			NOT = { has_country_flag = changed_from_colonial_nation }
			OR = {
				is_former_colonial_nation = yes
				is_colonial_nation = yes
			}
			colonial_brazil = {
				owned_by = ROOT
			}
			OR = {
				ai = no
				num_of_cities = 3	
			}
		}
		provinces_to_highlight = {
			OR = {
				province_id = 748	# Belem
				province_id = 2935	# Gurupa
				province_id = 2929	# Para
				province_id = 7558	# Chaves
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			adm_tech = 10
			is_former_colonial_nation = yes
			is_free_or_tributary_trigger = yes
			is_at_war = no
			NOT = { exists = AMZ }
			owns_core_province = 748	# Belem
			owns_core_province = 2935	# Gurupa
			owns_core_province = 2929	# Para
			owns_core_province = 7558	# Chaves
		}
		effect = {
			change_tag = AMZ
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = { NOT = { government_rank = 2 } }
				set_government_rank = 2
			}
			colonial_brazil = {
				limit = {
					NOT = {
						is_permanent_claim = ROOT
						is_core = ROOT
					}
				}
				add_permanent_claim = AMZ
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			add_prestige = 25
			set_country_flag = changed_from_colonial_nation
		}
		ai_will_do = {
			factor = 1
		}
	}
}