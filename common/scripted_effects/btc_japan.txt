increase_shogun_authority_scripted_effect = {
  1020 = {
    change_variable = { which = shogun_authority_var value = $VALUE$ }
    if = {
      limit = { check_variable = { which = shogun_authority_var value = 100 } }
      set_variable = { which = shogun_authority_var value = 100 }
    }
	else_if = {
      limit = { NOT = { check_variable = { which = shogun_authority_var value = 0 } } }
      set_variable = { which = shogun_authority_var value = 0 }
    }
  }
}